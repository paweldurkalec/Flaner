# Flaner — Flat Planner

Flaner is a small desktop application to load a flat plan/sketch, set a scale, and display a grid matched to that scale. This repository contains an initial Python + pygame implementation.

Quick run

```bash
python -m pip install -r requirements.txt
python src/flaner.py
```

Docs: see `docs/README.md` for overview and the usage guide in `docs/usage.md`.

